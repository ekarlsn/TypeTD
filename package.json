{
    "devDependencies": {
        "elm": "^0.19.1-5",
        "elm-review": "^2.7.2"
    },
    "scripts": {
        "build": "rm -rf dist && cp -r static dist && npx elm make src/Main.elm --output dist/main.js"
    }
}
